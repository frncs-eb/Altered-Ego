package util;

/**
 * Represents the modes of the game.
 */
public enum GameMode {
    /** Human player vs human player. */
    VS_PLAYER,

    /** Human player vs cpu player. */
    VS_COMPUTER,

    /** Human player vs series of cpu player. */
    ARCADE
}
